{
    "name": "{{ constants.firstName }} {{ constants.lastName }}",
    "short_name": "{{ constants.firstName }} {{ constants.lastName }}",
    "icons": [
        {
            "src": "/{{ buildConstants.androidIconPrefix }}-192x192{{ buildConstants.pngExtension}}?v={{ buildConstants.siteVersion}}",
            "sizes": "192x192",
            "type": "image/png"
        },
        {
            "src": "/{{ buildConstants.androidIconPrefix }}-512x512{{ buildConstants.pngExtension }}?v={{ buildConstants.siteVersion }}",
            "sizes": "512x512",
            "type": "image/png"
        }
    ],
    "theme_color": "{{ buildConstants.themeColor }}",
    "background_color": "{{ buildConstants.themeColor }}"
}
